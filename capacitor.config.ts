import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.repayment.ionic',
  appName: 'repayment-ionic-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
